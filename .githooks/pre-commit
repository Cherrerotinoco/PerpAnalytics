#!/bin/sh
# pre-commit hook — runs format + lint before every commit.
# If either step fails the commit is aborted.

set -e

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

echo "▶ pnpm format..."
pnpm format

echo "▶ pnpm lint..."
pnpm lint

echo "✓ pre-commit checks passed"
